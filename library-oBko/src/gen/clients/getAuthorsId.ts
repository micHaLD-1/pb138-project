/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetAuthorsIdQueryResponse,
  GetAuthorsIdPathParams,
} from "../types/GetAuthorsId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetAuthorsIdUrl(id: GetAuthorsIdPathParams["id"]) {
  const res = { method: "GET", url: `/api/authors/${id}` as const };
  return res;
}

/**
 * @summary Get author by id
 * {@link /authors/:id}
 */
export async function getAuthorsId(
  id: GetAuthorsIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetAuthorsIdQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetAuthorsIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
