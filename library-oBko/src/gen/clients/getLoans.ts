/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetLoansQueryResponse,
  GetLoansQueryParams,
} from "../types/GetLoans.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetLoansUrl() {
  const res = { method: "GET", url: `/api/loans` as const };
  return res;
}

/**
 * @summary List loans
 * {@link /loans}
 */
export async function getLoans(
  params: GetLoansQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetLoansQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetLoansUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
