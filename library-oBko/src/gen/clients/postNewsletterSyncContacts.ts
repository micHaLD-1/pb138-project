/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  PostNewsletterSyncContactsMutationResponse,
  PostNewsletterSyncContacts403,
} from "../types/PostNewsletterSyncContacts.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getPostNewsletterSyncContactsUrl() {
  const res = { method: "POST", url: `/api/newsletter/sync-contacts` as const };
  return res;
}

/**
 * @summary Sync all users to Brevo contact list
 * {@link /newsletter/sync-contacts}
 */
export async function postNewsletterSyncContacts(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostNewsletterSyncContactsMutationResponse,
    ResponseErrorConfig<PostNewsletterSyncContacts403>,
    unknown
  >({
    method: "POST",
    url: getPostNewsletterSyncContactsUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
