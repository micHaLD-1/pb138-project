/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetNewsletterIdQueryResponse,
  GetNewsletterIdPathParams,
  GetNewsletterId403,
} from "../types/GetNewsletterId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetNewsletterIdUrl(id: GetNewsletterIdPathParams["id"]) {
  const res = { method: "GET", url: `/api/newsletter/${id}` as const };
  return res;
}

/**
 * @summary Get newsletter details
 * {@link /newsletter/:id}
 */
export async function getNewsletterId(
  id: GetNewsletterIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetNewsletterIdQueryResponse,
    ResponseErrorConfig<GetNewsletterId403>,
    unknown
  >({
    method: "GET",
    url: getGetNewsletterIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
