/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  PostNewsletterIdSendMutationResponse,
  PostNewsletterIdSendPathParams,
  PostNewsletterIdSend403,
} from "../types/PostNewsletterIdSend.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getPostNewsletterIdSendUrl(id: PostNewsletterIdSendPathParams["id"]) {
  const res = { method: "POST", url: `/api/newsletter/${id}/send` as const };
  return res;
}

/**
 * @summary Send newsletter campaign via Brevo
 * {@link /newsletter/:id/send}
 */
export async function postNewsletterIdSend(
  id: PostNewsletterIdSendPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PostNewsletterIdSendMutationResponse,
    ResponseErrorConfig<PostNewsletterIdSend403>,
    unknown
  >({
    method: "POST",
    url: getPostNewsletterIdSendUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
