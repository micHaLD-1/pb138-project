/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetLoansIdQueryResponse,
  GetLoansIdPathParams,
} from "../types/GetLoansId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetLoansIdUrl(id: GetLoansIdPathParams["id"]) {
  const res = { method: "GET", url: `/api/loans/${id}` as const };
  return res;
}

/**
 * @summary Get loan by id
 * {@link /loans/:id}
 */
export async function getLoansId(
  id: GetLoansIdPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetLoansIdQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetLoansIdUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
