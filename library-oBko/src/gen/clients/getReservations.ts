/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetReservationsQueryResponse,
  GetReservationsQueryParams,
} from "../types/GetReservations.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getGetReservationsUrl() {
  const res = { method: "GET", url: `/api/reservations` as const };
  return res;
}

/**
 * @summary List reservations
 * {@link /reservations}
 */
export async function getReservations(
  params: GetReservationsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetReservationsQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getGetReservationsUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
