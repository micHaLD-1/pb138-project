/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetGenresIdQueryResponse,
  GetGenresIdPathParams,
} from "../types/GetGenresId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getGenresId } from "../clients/getGenresId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getGenresIdQueryKey = (
  id: GetGenresIdPathParams["id"] | undefined,
) => [{ url: "/genres/:id", params: { id: id } }] as const;

export type GetGenresIdQueryKey = ReturnType<typeof getGenresIdQueryKey>;

export function getGenresIdQueryOptions(
  id: GetGenresIdPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getGenresIdQueryKey(id);
  return queryOptions<
    GetGenresIdQueryResponse,
    ResponseErrorConfig<Error>,
    GetGenresIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getGenresId(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get genre by id
 * {@link /genres/:id}
 */
export function useGetGenresId<
  TData = GetGenresIdQueryResponse,
  TQueryData = GetGenresIdQueryResponse,
  TQueryKey extends QueryKey = GetGenresIdQueryKey,
>(
  id: GetGenresIdPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetGenresIdQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getGenresIdQueryKey(id);

  const query = useQuery(
    {
      ...getGenresIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
