/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetBranchesQueryResponse,
  GetBranchesQueryParams,
} from "../types/GetBranches.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getBranches } from "../clients/getBranches.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getBranchesQueryKey = (params: GetBranchesQueryParams) =>
  [{ url: "/branches" }, ...(params ? [params] : [])] as const;

export type GetBranchesQueryKey = ReturnType<typeof getBranchesQueryKey>;

export function getBranchesQueryOptions(
  params: GetBranchesQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getBranchesQueryKey(params);
  return queryOptions<
    GetBranchesQueryResponse,
    ResponseErrorConfig<Error>,
    GetBranchesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getBranches(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List branches
 * {@link /branches}
 */
export function useGetBranches<
  TData = GetBranchesQueryResponse,
  TQueryData = GetBranchesQueryResponse,
  TQueryKey extends QueryKey = GetBranchesQueryKey,
>(
  params: GetBranchesQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetBranchesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getBranchesQueryKey(params);

  const query = useQuery(
    {
      ...getBranchesQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
