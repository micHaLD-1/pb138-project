/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetRulesQueryResponse } from "../types/GetRules.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getRules } from "../clients/getRules.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getRulesSuspenseQueryKey = () => [{ url: "/rules" }] as const;

export type GetRulesSuspenseQueryKey = ReturnType<
  typeof getRulesSuspenseQueryKey
>;

export function getRulesSuspenseQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getRulesSuspenseQueryKey();
  return queryOptions<
    GetRulesQueryResponse,
    ResponseErrorConfig<Error>,
    GetRulesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getRules({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get rules
 * {@link /rules}
 */
export function useGetRulesSuspense<
  TData = GetRulesQueryResponse,
  TQueryKey extends QueryKey = GetRulesSuspenseQueryKey,
>(
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetRulesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getRulesSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getRulesSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
