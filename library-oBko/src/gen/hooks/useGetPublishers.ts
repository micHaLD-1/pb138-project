/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetPublishersQueryResponse,
  GetPublishersQueryParams,
} from "../types/GetPublishers.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getPublishers } from "../clients/getPublishers.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getPublishersQueryKey = (params: GetPublishersQueryParams) =>
  [{ url: "/publishers" }, ...(params ? [params] : [])] as const;

export type GetPublishersQueryKey = ReturnType<typeof getPublishersQueryKey>;

export function getPublishersQueryOptions(
  params: GetPublishersQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPublishersQueryKey(params);
  return queryOptions<
    GetPublishersQueryResponse,
    ResponseErrorConfig<Error>,
    GetPublishersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getPublishers(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List publishers
 * {@link /publishers}
 */
export function useGetPublishers<
  TData = GetPublishersQueryResponse,
  TQueryData = GetPublishersQueryResponse,
  TQueryKey extends QueryKey = GetPublishersQueryKey,
>(
  params: GetPublishersQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetPublishersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getPublishersQueryKey(params);

  const query = useQuery(
    {
      ...getPublishersQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
