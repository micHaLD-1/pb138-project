/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetLoansQueryResponse,
  GetLoansQueryParams,
} from "../types/GetLoans.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getLoans } from "../clients/getLoans.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getLoansSuspenseQueryKey = (params: GetLoansQueryParams) =>
  [{ url: "/loans" }, ...(params ? [params] : [])] as const;

export type GetLoansSuspenseQueryKey = ReturnType<
  typeof getLoansSuspenseQueryKey
>;

export function getLoansSuspenseQueryOptions(
  params: GetLoansQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getLoansSuspenseQueryKey(params);
  return queryOptions<
    GetLoansQueryResponse,
    ResponseErrorConfig<Error>,
    GetLoansQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getLoans(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List loans
 * {@link /loans}
 */
export function useGetLoansSuspense<
  TData = GetLoansQueryResponse,
  TQueryKey extends QueryKey = GetLoansSuspenseQueryKey,
>(
  params: GetLoansQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetLoansQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getLoansSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getLoansSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
