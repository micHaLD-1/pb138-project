/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetNewsletterQueryResponse,
  GetNewsletterQueryParams,
  GetNewsletter403,
} from "../types/GetNewsletter.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getNewsletter } from "../clients/getNewsletter.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getNewsletterQueryKey = (params: GetNewsletterQueryParams) =>
  [{ url: "/newsletter" }, ...(params ? [params] : [])] as const;

export type GetNewsletterQueryKey = ReturnType<typeof getNewsletterQueryKey>;

export function getNewsletterQueryOptions(
  params: GetNewsletterQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getNewsletterQueryKey(params);
  return queryOptions<
    GetNewsletterQueryResponse,
    ResponseErrorConfig<GetNewsletter403>,
    GetNewsletterQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getNewsletter(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List newsletters
 * {@link /newsletter}
 */
export function useGetNewsletter<
  TData = GetNewsletterQueryResponse,
  TQueryData = GetNewsletterQueryResponse,
  TQueryKey extends QueryKey = GetNewsletterQueryKey,
>(
  params: GetNewsletterQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetNewsletterQueryResponse,
        ResponseErrorConfig<GetNewsletter403>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getNewsletterQueryKey(params);

  const query = useQuery(
    {
      ...getNewsletterQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetNewsletter403>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
