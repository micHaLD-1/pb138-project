/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetLoansQueryResponse,
  GetLoansQueryParams,
} from "../types/GetLoans.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getLoans } from "../clients/getLoans.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getLoansQueryKey = (params: GetLoansQueryParams) =>
  [{ url: "/loans" }, ...(params ? [params] : [])] as const;

export type GetLoansQueryKey = ReturnType<typeof getLoansQueryKey>;

export function getLoansQueryOptions(
  params: GetLoansQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getLoansQueryKey(params);
  return queryOptions<
    GetLoansQueryResponse,
    ResponseErrorConfig<Error>,
    GetLoansQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getLoans(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List loans
 * {@link /loans}
 */
export function useGetLoans<
  TData = GetLoansQueryResponse,
  TQueryData = GetLoansQueryResponse,
  TQueryKey extends QueryKey = GetLoansQueryKey,
>(
  params: GetLoansQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetLoansQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getLoansQueryKey(params);

  const query = useQuery(
    {
      ...getLoansQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
