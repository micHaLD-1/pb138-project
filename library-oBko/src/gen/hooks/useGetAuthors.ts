/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetAuthorsQueryResponse,
  GetAuthorsQueryParams,
} from "../types/GetAuthors.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getAuthors } from "../clients/getAuthors.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getAuthorsQueryKey = (params: GetAuthorsQueryParams) =>
  [{ url: "/authors" }, ...(params ? [params] : [])] as const;

export type GetAuthorsQueryKey = ReturnType<typeof getAuthorsQueryKey>;

export function getAuthorsQueryOptions(
  params: GetAuthorsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getAuthorsQueryKey(params);
  return queryOptions<
    GetAuthorsQueryResponse,
    ResponseErrorConfig<Error>,
    GetAuthorsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getAuthors(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List authors
 * {@link /authors}
 */
export function useGetAuthors<
  TData = GetAuthorsQueryResponse,
  TQueryData = GetAuthorsQueryResponse,
  TQueryKey extends QueryKey = GetAuthorsQueryKey,
>(
  params: GetAuthorsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetAuthorsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getAuthorsQueryKey(params);

  const query = useQuery(
    {
      ...getAuthorsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
