/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetReservationsQueryResponse,
  GetReservationsQueryParams,
} from "../types/GetReservations.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getReservations } from "../clients/getReservations.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getReservationsQueryKey = (params: GetReservationsQueryParams) =>
  [{ url: "/reservations" }, ...(params ? [params] : [])] as const;

export type GetReservationsQueryKey = ReturnType<
  typeof getReservationsQueryKey
>;

export function getReservationsQueryOptions(
  params: GetReservationsQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getReservationsQueryKey(params);
  return queryOptions<
    GetReservationsQueryResponse,
    ResponseErrorConfig<Error>,
    GetReservationsQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getReservations(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List reservations
 * {@link /reservations}
 */
export function useGetReservations<
  TData = GetReservationsQueryResponse,
  TQueryData = GetReservationsQueryResponse,
  TQueryKey extends QueryKey = GetReservationsQueryKey,
>(
  params: GetReservationsQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetReservationsQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getReservationsQueryKey(params);

  const query = useQuery(
    {
      ...getReservationsQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
