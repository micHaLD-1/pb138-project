/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetRulesQueryResponse } from "../types/GetRules.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getRules } from "../clients/getRules.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getRulesQueryKey = () => [{ url: "/rules" }] as const;

export type GetRulesQueryKey = ReturnType<typeof getRulesQueryKey>;

export function getRulesQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getRulesQueryKey();
  return queryOptions<
    GetRulesQueryResponse,
    ResponseErrorConfig<Error>,
    GetRulesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getRules({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get rules
 * {@link /rules}
 */
export function useGetRules<
  TData = GetRulesQueryResponse,
  TQueryData = GetRulesQueryResponse,
  TQueryKey extends QueryKey = GetRulesQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetRulesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getRulesQueryKey();

  const query = useQuery(
    {
      ...getRulesQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
