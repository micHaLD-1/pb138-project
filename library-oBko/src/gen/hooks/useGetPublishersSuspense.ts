/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetPublishersQueryResponse,
  GetPublishersQueryParams,
} from "../types/GetPublishers.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  UseSuspenseQueryOptions,
  UseSuspenseQueryResult,
} from "@tanstack/react-query";
import { getPublishers } from "../clients/getPublishers.ts";
import { queryOptions, useSuspenseQuery } from "@tanstack/react-query";

export const getPublishersSuspenseQueryKey = (
  params: GetPublishersQueryParams,
) => [{ url: "/publishers" }, ...(params ? [params] : [])] as const;

export type GetPublishersSuspenseQueryKey = ReturnType<
  typeof getPublishersSuspenseQueryKey
>;

export function getPublishersSuspenseQueryOptions(
  params: GetPublishersQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getPublishersSuspenseQueryKey(params);
  return queryOptions<
    GetPublishersQueryResponse,
    ResponseErrorConfig<Error>,
    GetPublishersQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getPublishers(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List publishers
 * {@link /publishers}
 */
export function useGetPublishersSuspense<
  TData = GetPublishersQueryResponse,
  TQueryKey extends QueryKey = GetPublishersSuspenseQueryKey,
>(
  params: GetPublishersQueryParams,
  options: {
    query?: Partial<
      UseSuspenseQueryOptions<
        GetPublishersQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey =
    resolvedOptions?.queryKey ?? getPublishersSuspenseQueryKey(params);

  const query = useSuspenseQuery(
    {
      ...getPublishersSuspenseQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient,
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
