/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetBooksIdQueryResponse,
  GetBooksIdPathParams,
} from "../types/GetBooksId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getBooksId } from "../clients/getBooksId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getBooksIdQueryKey = (
  id: GetBooksIdPathParams["id"] | undefined,
) => [{ url: "/books/:id", params: { id: id } }] as const;

export type GetBooksIdQueryKey = ReturnType<typeof getBooksIdQueryKey>;

export function getBooksIdQueryOptions(
  id: GetBooksIdPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getBooksIdQueryKey(id);
  return queryOptions<
    GetBooksIdQueryResponse,
    ResponseErrorConfig<Error>,
    GetBooksIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getBooksId(id!, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary Get book by id
 * {@link /books/:id}
 */
export function useGetBooksId<
  TData = GetBooksIdQueryResponse,
  TQueryData = GetBooksIdQueryResponse,
  TQueryKey extends QueryKey = GetBooksIdQueryKey,
>(
  id: GetBooksIdPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetBooksIdQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getBooksIdQueryKey(id);

  const query = useQuery(
    {
      ...getBooksIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
