/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetNewsletterIdQueryResponse,
  GetNewsletterIdPathParams,
  GetNewsletterId403,
} from "../types/GetNewsletterId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getNewsletterId } from "../clients/getNewsletterId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getNewsletterIdQueryKey = (
  id: GetNewsletterIdPathParams["id"] | undefined,
) => [{ url: "/newsletter/:id", params: { id: id } }] as const;

export type GetNewsletterIdQueryKey = ReturnType<
  typeof getNewsletterIdQueryKey
>;

export function getNewsletterIdQueryOptions(
  id: GetNewsletterIdPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getNewsletterIdQueryKey(id);
  return queryOptions<
    GetNewsletterIdQueryResponse,
    ResponseErrorConfig<GetNewsletterId403>,
    GetNewsletterIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getNewsletterId(id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get newsletter details
 * {@link /newsletter/:id}
 */
export function useGetNewsletterId<
  TData = GetNewsletterIdQueryResponse,
  TQueryData = GetNewsletterIdQueryResponse,
  TQueryKey extends QueryKey = GetNewsletterIdQueryKey,
>(
  id: GetNewsletterIdPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetNewsletterIdQueryResponse,
        ResponseErrorConfig<GetNewsletterId403>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getNewsletterIdQueryKey(id);

  const query = useQuery(
    {
      ...getNewsletterIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetNewsletterId403>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
