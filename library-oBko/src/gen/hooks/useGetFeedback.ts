/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetFeedbackQueryResponse,
  GetFeedbackQueryParams,
  GetFeedback403,
} from "../types/GetFeedback.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getFeedback } from "../clients/getFeedback.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getFeedbackQueryKey = (params: GetFeedbackQueryParams) =>
  [{ url: "/feedback" }, ...(params ? [params] : [])] as const;

export type GetFeedbackQueryKey = ReturnType<typeof getFeedbackQueryKey>;

export function getFeedbackQueryOptions(
  params: GetFeedbackQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getFeedbackQueryKey(params);
  return queryOptions<
    GetFeedbackQueryResponse,
    ResponseErrorConfig<GetFeedback403>,
    GetFeedbackQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getFeedback(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List feedbacks
 * {@link /feedback}
 */
export function useGetFeedback<
  TData = GetFeedbackQueryResponse,
  TQueryData = GetFeedbackQueryResponse,
  TQueryKey extends QueryKey = GetFeedbackQueryKey,
>(
  params: GetFeedbackQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetFeedbackQueryResponse,
        ResponseErrorConfig<GetFeedback403>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getFeedbackQueryKey(params);

  const query = useQuery(
    {
      ...getFeedbackQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<GetFeedback403>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
