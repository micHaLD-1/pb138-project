/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetGenresQueryResponse,
  GetGenresQueryParams,
} from "../types/GetGenres.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getGenres } from "../clients/getGenres.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getGenresQueryKey = (params: GetGenresQueryParams) =>
  [{ url: "/genres" }, ...(params ? [params] : [])] as const;

export type GetGenresQueryKey = ReturnType<typeof getGenresQueryKey>;

export function getGenresQueryOptions(
  params: GetGenresQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getGenresQueryKey(params);
  return queryOptions<
    GetGenresQueryResponse,
    ResponseErrorConfig<Error>,
    GetGenresQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getGenres(params, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @summary List genres
 * {@link /genres}
 */
export function useGetGenres<
  TData = GetGenresQueryResponse,
  TQueryData = GetGenresQueryResponse,
  TQueryKey extends QueryKey = GetGenresQueryKey,
>(
  params: GetGenresQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetGenresQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getGenresQueryKey(params);

  const query = useQuery(
    {
      ...getGenresQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
