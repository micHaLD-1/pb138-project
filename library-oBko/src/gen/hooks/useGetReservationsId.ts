/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetReservationsIdQueryResponse,
  GetReservationsIdPathParams,
} from "../types/GetReservationsId.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getReservationsId } from "../clients/getReservationsId.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getReservationsIdQueryKey = (
  id: GetReservationsIdPathParams["id"] | undefined,
) => [{ url: "/reservations/:id", params: { id: id } }] as const;

export type GetReservationsIdQueryKey = ReturnType<
  typeof getReservationsIdQueryKey
>;

export function getReservationsIdQueryOptions(
  id: GetReservationsIdPathParams["id"] | undefined,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getReservationsIdQueryKey(id);
  return queryOptions<
    GetReservationsIdQueryResponse,
    ResponseErrorConfig<Error>,
    GetReservationsIdQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      return getReservationsId(id!, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary Get reservation by id
 * {@link /reservations/:id}
 */
export function useGetReservationsId<
  TData = GetReservationsIdQueryResponse,
  TQueryData = GetReservationsIdQueryResponse,
  TQueryKey extends QueryKey = GetReservationsIdQueryKey,
>(
  id: GetReservationsIdPathParams["id"] | undefined,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetReservationsIdQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getReservationsIdQueryKey(id);

  const query = useQuery(
    {
      ...getReservationsIdQueryOptions(id, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
