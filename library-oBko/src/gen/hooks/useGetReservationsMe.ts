/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  GetReservationsMeQueryResponse,
  GetReservationsMeQueryParams,
} from "../types/GetReservationsMe.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { getReservationsMe } from "../clients/getReservationsMe.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getReservationsMeQueryKey = (params: GetReservationsMeQueryParams) =>
  [{ url: "/reservations/me" }, ...(params ? [params] : [])] as const;

export type GetReservationsMeQueryKey = ReturnType<
  typeof getReservationsMeQueryKey
>;

export function getReservationsMeQueryOptions(
  params: GetReservationsMeQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getReservationsMeQueryKey(params);
  return queryOptions<
    GetReservationsMeQueryResponse,
    ResponseErrorConfig<Error>,
    GetReservationsMeQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getReservationsMe(params, {
        ...config,
        signal: config.signal ?? signal,
      });
    },
  });
}

/**
 * @summary List current user's reservations
 * {@link /reservations/me}
 */
export function useGetReservationsMe<
  TData = GetReservationsMeQueryResponse,
  TQueryData = GetReservationsMeQueryResponse,
  TQueryKey extends QueryKey = GetReservationsMeQueryKey,
>(
  params: GetReservationsMeQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetReservationsMeQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getReservationsMeQueryKey(params);

  const query = useQuery(
    {
      ...getReservationsMeQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
